using System;
using UnityEngine;

///Need to give information when targeting an object need to rethink it
[Serializable]
public class EntityData
{
    [SerializeField] private string entityName;
    [SerializeField] private string entityDescription;
}